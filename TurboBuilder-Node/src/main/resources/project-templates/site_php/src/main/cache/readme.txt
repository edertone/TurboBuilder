- This directory contains the project cache data which is generated at runtime.

- Directory contents can be deleted at any time to reset the cache.

- Do not delete the directory itself.

- Directory is managed by the project cache mechanism, do not use it for other purposes.